<%= form_for(@project) do |f| %>
  <div class="field">
    <%= f.label :name, "Project Name:" %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
